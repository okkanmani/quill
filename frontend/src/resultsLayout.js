/** Default results list width (matches legacy max-w-3xl). */
export const RESULTS_LIST_WIDTH_CLASS = "max-w-3xl";

/**
 * Split view: list at 75% of list-only width, detail panel at 100% of that width (3fr : 4fr).
 * Total width = 1.75 × list-only width.
 */
export const RESULTS_SPLIT_WIDTH_CLASS = "max-w-[84rem]";

export const RESULTS_SPLIT_GRID_CLASS =
  "lg:grid lg:grid-cols-[3fr_4fr] lg:gap-6 lg:items-start";
