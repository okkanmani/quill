/** Shared type scale for admin Worksheets page (tree, cards, unassigned). */

export const WS_PAGE_HEADING = "text-xl font-bold text-slate-950";

/** “Unassigned”, sort labels */
export const WS_EYEBROW = "text-xs font-semibold uppercase tracking-wide text-slate-600";

/** Practice, Timed, nested sections, week/subject group titles */
export const WS_SECTION_TITLE = "text-sm font-semibold text-slate-950";

/** “3 worksheets · 2 sections”, done counts */
export const WS_SECTION_META = "text-xs font-medium text-slate-600 tabular-nums";

/** Worksheet card title (admin compact rows) */
export const WS_CARD_TITLE = "text-sm font-semibold text-slate-900 leading-snug";

/** Worksheet card title (student / spacious rows) */
export const WS_CARD_TITLE_LOOSE = "text-lg font-semibold text-slate-900";

/** Footer row: question count, secondary hints */
export const WS_CARD_DETAIL = "text-xs text-slate-600";

export const WS_CARD_DETAIL_LOOSE = "text-sm text-indigo-600";

/** Expand/collapse chevrons in section headers */
export const WS_CHEVRON = "text-xs font-bold text-slate-700 tabular-nums";

/** Empty states, helper copy */
export const WS_BODY = "text-sm text-slate-600";

export const WS_BODY_MUTED = "text-sm text-slate-500";

/** Admin worksheet list: pad container, pull rows back into padding (avoids overflow clip). */
export const WS_ADMIN_WORKSHEET_INSET = "pl-2";

export const WS_ADMIN_WORKSHEET_ROW =
  "gap-0.5 -ml-4";
